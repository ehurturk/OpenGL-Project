
/*
 * Procedure for buffers:
 * 
 * 1) Vertex Buffer Objects (VBO):
 *      *) glGenBuffers(1, &vbo);
 *      *) glBindBuffer(GL_VERTEX_BUFFER);
 *      *) glBufferData(GL_VERTEX_BUFFER, GL_FLOAT, );

*/

#pragma once

class Renderer 
{
    public:
        
        void render();

    private:
        
        
};

